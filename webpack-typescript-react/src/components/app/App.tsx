import React from 'react';

export default function App(): React.JSX.Element {
    const test = 1;

    console.log(test);
    if (test === 1) {
        return <>React App Started</>;
    }

    return <>React App Started</>;
}
